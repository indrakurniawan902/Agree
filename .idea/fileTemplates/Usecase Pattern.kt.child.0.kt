#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME}

#end
#parse("File Header.java")
/**
 * ${NAME} Usecase are ${NAME} Domain Contract Abstraction
 * on Domain Layer
 * @author: @telkomdev-abdul
 * @since: ${DAY} ${MONTH_NAME_SHORT} ${YEAR}
 */
interface ${NAME}Usecase {
    
}